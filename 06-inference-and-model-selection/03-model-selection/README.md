# Experiment 03 — Model Selection Shootout

Runs the same workload against multiple local models. Manually score `quality_score_manual` using a task-specific rubric before drawing conclusions.

Configure with `MODEL_NAMES="model-a,model-b,model-c"`.
